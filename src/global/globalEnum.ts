export enum HttpStatus{
    ERROR = 404,
    SUCCESS = 200
}

export enum HttpMessage{
    ERROR = 'Not Found',
    SUCCESS = 'Server Response Successfully'
}

export enum Role{
    User = 'ROLE_USER',
    Admin = 'ROLE_ADMIN',
}